// Phosphor icons imported as raw SVG markup, so only the icons actually used
// are bundled. Add new ones here and render with <Icon svg={icons.name} />.
import house from '@phosphor-icons/core/assets/regular/house.svg?raw'
import x from '@phosphor-icons/core/assets/regular/x.svg?raw'

export const icons = { house, x }
