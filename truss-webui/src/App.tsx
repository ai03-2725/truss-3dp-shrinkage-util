/**
 * Placeholder root for the development harness.
 *
 * This is replaced by the custom element shell (`<truss-calibrator>`) in T12 and
 * the real screens from T15 onwards.
 */
function App() {
  return <div class="container">Truss Calibrator</div>
}

export default App
