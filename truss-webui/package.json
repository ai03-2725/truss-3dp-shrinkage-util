{
  "name": "truss-webui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-images.ts && vite",
    "build": "node scripts/generate-images.ts && tsc -b && vite build",
    "test": "node scripts/generate-images.ts && node --test 'src/**/*.test.ts' 'scripts/**/*.test.ts'",
    "generate:images": "node scripts/generate-images.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "@phosphor-icons/core": "^2.1.1",
    "solid-js": "^1.9.15"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "sharp": "^0.35.4",
    "typescript": "~6.0.2",
    "vite": "^8.3.0",
    "vite-plugin-solid": "^2.11.14"
  }
}
